(set-info :status unsat)
(set-logic QF_UFLIA)
; f(x) = x + 1 and f(x) = x is contradictory (since x+1 != x for all integers)
(declare-fun f (Int) Int)
(declare-const x Int)
(assert (= (f x) (+ x 1)))
(assert (= (f x) x))
(check-sat)
(exit)
