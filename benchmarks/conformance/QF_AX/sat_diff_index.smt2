(set-info :status sat)
(set-logic QF_AX)
(declare-sort Idx 0)
(declare-sort Elm 0)
(declare-const a (Array Idx Elm))
(declare-const i Idx)
(declare-const j Idx)
(declare-const v Elm)
(assert (not (= i j)))
(assert (= (select (store a i v) j) (select a j)))
(check-sat)
(exit)
