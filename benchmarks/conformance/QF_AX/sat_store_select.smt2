(set-info :status sat)
(set-logic QF_AX)
(declare-sort Idx 0)
(declare-sort Elm 0)
(declare-const a (Array Idx Elm))
(declare-const i Idx)
(declare-const v Elm)
(assert (= (select (store a i v) i) v))
(check-sat)
(exit)
