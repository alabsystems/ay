(set-info :status sat)
(set-logic QF_AUFLIA)
(declare-const a (Array Int Int))
(declare-fun f (Int) Int)
(declare-const i Int)
(assert (= (select a i) (f i)))
(assert (= (f 0) 42))
(assert (= i 0))
(check-sat)
(exit)
