(set-info :status sat)
(set-logic QF_AUFLIA)
(declare-fun f (Int) Int)
(declare-const a (Array Int Int))
(declare-const x Int)
(assert (> x 0))
(assert (= (f x) (+ x 1)))
(assert (= (select a (f x)) 42))
(check-sat)
(exit)
