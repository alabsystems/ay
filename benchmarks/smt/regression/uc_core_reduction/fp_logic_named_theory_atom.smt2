(set-logic QF_FP)
(set-option :produce-unsat-cores true)
(declare-const p Bool)
(declare-const x Float32)
(assert (! p :named a1))
(assert (! (not p) :named a2))
(assert (! (fp.isNaN x) :named a3))
(check-sat)
(get-unsat-core)
