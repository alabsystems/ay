(set-logic QF_UF)
(set-option :produce-unsat-cores true)
(declare-const p Bool)
(declare-const u Bool)
(assert (! p :named a1))
(assert (! (not p) :named a2))
(assert (! u :named a3))
(check-sat)
(get-unsat-core)
