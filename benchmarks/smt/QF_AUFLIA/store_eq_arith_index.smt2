(set-logic QF_AUFLIA)
; Two stores equal implies stored values equal (with arithmetic indices)
(declare-fun a () (Array Int Int))
(declare-fun b () (Array Int Int))
(declare-fun i () Int)
(declare-fun j () Int)
(declare-fun v () Int)
(declare-fun w () Int)
(assert (= i j))
(assert (= (store a i v) (store b j w)))
(assert (not (= v w)))
(check-sat)
; Expected: unsat (store congruence: equal stores at equal indices => equal values)
(exit)
